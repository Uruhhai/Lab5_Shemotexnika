module demux1to8 (
    input wire D,           // ??????? ??????
    input wire [2:0] S,     // ???????? (??????)
    input wire EN,          // ???? ??????? (???????? 0)
    output reg [7:0] Y      // 8 ???????
);

always @(*) begin
    if (EN) begin
        // ???? EN = 1, ?????????? ??? ?????? ? Z-????
        Y = 8'bzzzzzzzz;
    end else begin
        // ???????? ???????? ??? ?????? ? 0
        Y = 8'b00000000;
        // ????????? ???????? D ?? ??????? ?????
        Y[S] = D;
    end
end

endmodule
